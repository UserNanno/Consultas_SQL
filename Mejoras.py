Yo hice una version similar en solo SQL:

CREATE TABLE CATALOG_LHCL_PROD_BCP_EXPL.BCP_EDV_RBMBDN.T72496_SOLICITUDESCEF
AS
WITH
SOLICITUDES_CEF AS (
	SELECT
		CAST(CODMESEVALUACION AS INT) AS CODMESEVALUACION,
		CODEVALUACIONSOLICITUD,
		CODIDEVALUACION,
		FECEVALUACION,
		CODINTERNOCOMPUTACIONAL,
		NUMSOLICITUDEVALUACION,
		CODSECUENCIALDECISIONRBM,
		DESDECISIONEVALUACION,
		DESREGLAPAUTA,
		DESTIPDECISIONRESULTADORBM,
		DESEVALUACION,
		MTOCEMEVALUACION,
		DESCAMPANIASOLICITUD,
		DESTIPEVALUACIONSOLICITUDCREDITO,
		DESCANALVENTARBMPER,
		(CASE
			WHEN DESCANALVENTARBMPER = 'SALEFORCE' THEN SUBSTR(TRIM(NUMSOLICITUDEVALUACION), 5, 7)
			WHEN DESCANALVENTARBMPER = 'LOANS' THEN SUBSTR(TRIM(NUMSOLICITUDEVALUACION), 3, 8)
			ELSE TRIM(NUMSOLICITUDEVALUACION)
		END) AS NUMSOLICITUDCORTO,
		(CASE
			WHEN DESTIPDECISIONRESULTADORBM = 'Approve' THEN 1
			WHEN DESTIPDECISIONRESULTADORBM = 'Decline' THEN 3
			ELSE 2
		END) AS RESULTADOJERARQUIA,
		(CASE
			WHEN DESCANALVENTARBMPER = 'BANCA MÓVIL' AND DESEVALUACION = 'df_Regular' THEN 'NO VALIDO'
			WHEN DESCAMPANIASOLICITUD = 'Reactivo' THEN 'REACTIVO'
			ELSE 'NO REACTIVO'
		END) AS EVALUACIONREACTIVO,
		(CASE
			WHEN DESTIPEVALUACIONSOLICITUDCREDITO IN ('REGULAR LD', 'CUOTEALO') AND DESCAMPANIASOLICITUD IN ('100% Aprobado', 'Pre-Aprobado') THEN 'LD APROBADO'
			WHEN DESTIPEVALUACIONSOLICITUDCREDITO IN ('REGULAR LD', 'CUOTEALO') AND DESCAMPANIASOLICITUD = 'CEF Shield' THEN 'LD SHIELD'
			WHEN DESTIPEVALUACIONSOLICITUDCREDITO = 'REGULAR LD' AND DESCAMPANIASOLICITUD = 'Convenio' THEN 'LD CONVENIO'
			WHEN DESTIPEVALUACIONSOLICITUDCREDITO = 'COMPRA DE DEUDA' AND DESCAMPANIASOLICITUD IN ('100% Aprobado', 'Pre-Aprobado') THEN 'CDD APROBADO'
			WHEN DESTIPEVALUACIONSOLICITUDCREDITO = 'COMPRA DE DEUDA' AND DESCAMPANIASOLICITUD = 'Convenio' THEN 'CDD CONVENIO'
			WHEN DESTIPEVALUACIONSOLICITUDCREDITO = 'LD + CONSOLIDACION' AND DESCAMPANIASOLICITUD IN ('100% Aprobado', 'Pre-Aprobado') THEN 'REE APROBADO'
			WHEN DESTIPEVALUACIONSOLICITUDCREDITO = 'LD + CONSOLIDACION' AND DESCAMPANIASOLICITUD = 'Convenio' THEN 'REE CONVENIO'
			WHEN DESTIPEVALUACIONSOLICITUDCREDITO = 'LD + COMPRA DE DEUDA + CONSOLIDACION' AND DESCAMPANIASOLICITUD IN ('100% Aprobado', 'Pre-Aprobado') THEN 'CONS APROBADO'
			WHEN DESTIPEVALUACIONSOLICITUDCREDITO = 'LD + COMPRA DE DEUDA + CONSOLIDACION' AND DESCAMPANIASOLICITUD = 'Convenio' THEN 'CONS CONVENIO'
			ELSE 'REACTIVO'
		END) AS LEAD_CEF
	FROM CATALOG_LHCL_PROD_BCP.BCP_DDV_RBMRBMPER_MODELOGESTION_VU.MD_EVALUACIONSOLICITUDCREDITO
	WHERE TIPPRODUCTOSOLICITUDRBM = 'CC' AND DESCANALVENTARBMPER NOT IN ('YAPE', 'OTROS') AND CODINTERNOCOMPUTACIONAL IS NOT NULL AND CODINTERNOCOMPUTACIONAL != '' AND CAST(CODMESEVALUACION AS INT) >= 202301
),
BASE_RN AS (
	SELECT
		CODMESEVALUACION,
		CODEVALUACIONSOLICITUD,
		CODIDEVALUACION,
		FECEVALUACION,
		CODINTERNOCOMPUTACIONAL,
		NUMSOLICITUDEVALUACION,
		CODSECUENCIALDECISIONRBM,
		DESDECISIONEVALUACION,
		DESREGLAPAUTA,
		DESTIPDECISIONRESULTADORBM,
		DESEVALUACION,
		MTOCEMEVALUACION,
		DESCAMPANIASOLICITUD,
		DESTIPEVALUACIONSOLICITUDCREDITO,
		DESCANALVENTARBMPER,
		NUMSOLICITUDCORTO,
		RESULTADOJERARQUIA,
		EVALUACIONREACTIVO,
		LEAD_CEF
	FROM (
		SELECT
			*,
			ROW_NUMBER() OVER (
				PARTITION BY CODMESEVALUACION, CODINTERNOCOMPUTACIONAL, CODEVALUACIONSOLICITUD
				ORDER BY FECEVALUACION DESC, CODIDEVALUACION DESC
			) AS RN
		FROM SOLICITUDES_CEF
	)
	WHERE RN = 1
),
SOLICITUDES_UNICAS AS (
	SELECT
		CODMESEVALUACION,
		CODEVALUACIONSOLICITUD,
		CODIDEVALUACION,
		FECEVALUACION,
		CODINTERNOCOMPUTACIONAL,
		NUMSOLICITUDEVALUACION,
		CODSECUENCIALDECISIONRBM,
		DESDECISIONEVALUACION,
		DESREGLAPAUTA,
		DESTIPDECISIONRESULTADORBM,
		DESEVALUACION,
		MTOCEMEVALUACION,
		DESCAMPANIASOLICITUD,
		DESTIPEVALUACIONSOLICITUDCREDITO,
		DESCANALVENTARBMPER,
		NUMSOLICITUDCORTO,
		RESULTADOJERARQUIA,
		EVALUACIONREACTIVO,
		LEAD_CEF
	FROM (
		SELECT
			*,
			ROW_NUMBER() OVER (
				PARTITION BY CODMESEVALUACION, CODINTERNOCOMPUTACIONAL, NUMSOLICITUDEVALUACION
				ORDER BY RESULTADOJERARQUIA ASC, FECEVALUACION DESC
			) AS RN
		FROM BASE_RN
		WHERE EVALUACIONREACTIVO <> 'NO VALIDO'
	)
	WHERE RN = 1
)
SELECT
	CODMESEVALUACION,
	CODEVALUACIONSOLICITUD,
	CODIDEVALUACION,
	FECEVALUACION,
	CODINTERNOCOMPUTACIONAL,
	NUMSOLICITUDEVALUACION,
	CODSECUENCIALDECISIONRBM,
	DESDECISIONEVALUACION,
	DESREGLAPAUTA,
	DESTIPDECISIONRESULTADORBM,
	DESEVALUACION,
	MTOCEMEVALUACION,
	DESCAMPANIASOLICITUD,
	DESTIPEVALUACIONSOLICITUDCREDITO,
	DESCANALVENTARBMPER,
	NUMSOLICITUDCORTO,
	RESULTADOJERARQUIA,
	EVALUACIONREACTIVO,
	LEAD_CEF
FROM SOLICITUDES_UNICAS;





CREATE TABLE CATALOG_LHCL_PROD_BCP_EXPL.BCP_EDV_RBMBDN.T72496_TIPOCAMBIO
AS
WITH
TP_FECHA_TIPOCAMBIO AS (
	SELECT
		CAST(date_format(FECTIPCAMBIO, 'yyyyMM') AS INT) AS CODMES,
		MAX(FECTIPCAMBIO) AS FECTIPCAMBIO
	FROM CATALOG_LHCL_PROD_BCP.BCP_UDV_INT_VU.H_TIPOCAMBIO
	WHERE CAST(DATE_FORMAT(FECTIPCAMBIO, 'yyyMM') AS INT) >= 202301 AND CODMONEDADESTINO = '0001' AND CODAPP = 'GLM'
	GROUP BY CAST(date_format(FECTIPCAMBIO, 'yyyyMM') AS INT)
),
TP_DATA_TIPOCAMBIO AS (
	SELECT
		CAST(date_format(FECTIPCAMBIO, 'yyyyMM') AS INT) AS CODMES,
		FECTIPCAMBIO,
		CODMONEDAORIGEN,
		CODMONEDADESTINO,
		MTOCAMBIOMONEDAORIGENMONEDADESTINO
	FROM CATALOG_LHCL_PROD_BCP.BCP_UDV_INT_VU.H_TIPOCAMBIO
	WHERE CAST(DATE_FORMAT(FECTIPCAMBIO, 'yyyMM') AS INT) >= 202301 AND CODMONEDADESTINO = '0001' AND CODAPP = 'GLM'
),
TIPO_CAMBIO AS (
	SELECT
		A.CODMES,
		A.FECTIPCAMBIO,
		A.CODMONEDAORIGEN,
		A.CODMONEDADESTINO,
		A.MTOCAMBIOMONEDAORIGENMONEDADESTINO
	FROM TP_DATA_TIPOCAMBIO A
	INNER JOIN TP_FECHA_TIPOCAMBIO B ON (A.CODMES = B.CODMES AND A.FECTIPCAMBIO = B.FECTIPCAMBIO)
),
TIPO_CAMBIO_UNICO AS (
	SELECT
		CODMES,
		FECTIPCAMBIO,
		CODMONEDAORIGEN,
		CODMONEDADESTINO,
		MTOCAMBIOMONEDAORIGENMONEDADESTINO
	FROM (
		SELECT
			*,
			ROW_NUMBER() OVER (
				PARTITION BY CODMES, CODMONEDAORIGEN
				ORDER BY FECTIPCAMBIO DESC, CODMONEDAORIGEN
			) AS RN
		FROM TIPO_CAMBIO
	)
	WHERE RN = 1
)
SELECT
	CODMES,
	FECTIPCAMBIO,
	CODMONEDAORIGEN,
	CODMONEDADESTINO,
	MTOCAMBIOMONEDAORIGENMONEDADESTINO
FROM TIPO_CAMBIO_UNICO







CREATE TABLE CATALOG_LHCL_PROD_BCP_EXPL.BCP_EDV_RBMBDN.T72496_VENTASCEF
AS
WITH
VENTASCEF AS (
	SELECT
		CAST(date_format(A.FECAPERTURA, 'yyyyMM') AS INT) AS CODMES,
		CAST(A.FECAPERTURA AS DATE) AS FECAPERTURA,
		CAST(A.FECDESEMBOLSO AS DATE) AS FECDESEMBOLSO,
		A.CODCLAVECTA,
		A.CODSOLICITUD,
		A.CODCLAVEPARTYCLI,
		C.CODCLAVEUNICOCLI,
		UPPER(TRIM(C.CODINTERNOCOMPUTACIONAL)) AS CODINTERNOCOMPUTACIONAL,
		A.CODPRODUCTO,
		A.MTODESEMBOLSADO,
		A.CODMONEDA,
		(CASE
			WHEN A.CODMONEDA <> '0001' THEN A.MTODESEMBOLSADO * B.MTOCAMBIOMONEDAORIGENMONEDADESTINO
			ELSE A.MTODESEMBOLSADO
		END) AS MTODESEMBOLSADOSOL,
		(CASE
			WHEN SUBSTRING(TRIM(A.CODSOLICITUD), 1, 2) = 'CX' THEN SUBSTR(TRIM(A.CODSOLICITUD), 3, 8)
			WHEN SUBSTRING(TRIM(A.CODSOLICITUD), 1, 2) = 'DX' THEN SUBSTR(TRIM(A.CODSOLICITUD), 3, 8)
			WHEN A.CODSOLICITUD LIKE '2________' THEN SUBSTR(TRIM(A.CODSOLICITUD), 3, 7)
			WHEN A.CODSOLICITUD LIKE '      2________' THEN SUBSTR(TRIM(A.CODSOLICITUD), 3, 7)
			WHEN A.CODSOLICITUD LIKE 'O%' THEN SUBSTR(TRIM(A.CODSOLICITUD), 3, 7)
			ELSE TRIM(A.CODSOLICITUD)
		END) AS CODSOLICITUDCORTO,
		(CASE
			WHEN A.CODPRODUCTO = 'CPEFIA' THEN 'CUOTEALO'
			WHEN A.CODPRODUCTO = 'CPEYAP' THEN 'YAPE'
			WHEN SUBSTRING(TRIM(A.CODSOLICITUD), 1, 2) = 'PE' THEN 'CUOTEALO'
			WHEN SUBSTRING(TRIM(A.CODSOLICITUD), 1, 2) = 'YP' THEN 'YAPE'
			WHEN SUBSTRING(TRIM(A.CODSOLICITUD), 1, 2) = 'JB' THEN 'BANCA MÓVIL'
			WHEN SUBSTRING(TRIM(A.CODSOLICITUD), 1, 2) = 'CX' THEN 'LOANS'
			WHEN SUBSTRING(TRIM(A.CODSOLICITUD), 1, 2) = 'DX' THEN 'LOANS'
			WHEN A.CODSOLICITUD LIKE '2________' THEN 'SALEFORCE'
			WHEN A.CODSOLICITUD LIKE '      2________' THEN 'SALEFORCE'
			WHEN A.CODSOLICITUD LIKE 'O%' THEN 'SALEFORCE'
			ELSE 'OTROS'
		END) AS CANAL
	FROM CATALOG_LHCL_PROD_BCP.BCP_UDV_INT_VU.M_CUENTACREDITOPERSONAL A
	LEFT JOIN CATALOG_LHCL_PROD_BCP_EXPL.BCP_EDV_RBMBDN.T72496_TIPOCAMBIO B ON (CAST(date_format(A.FECAPERTURA, 'yyyyMM') AS INT) = B.CODMES AND A.CODMONEDA = B.CODMONEDAORIGEN)
	LEFT JOIN CATALOG_LHCL_PROD_BCP.BCP_UDV_INT_VU.M_CLIENTE C ON (A.CODCLAVEPARTYCLI = C.CODCLAVEPARTYCLI)
	WHERE A.FLGREGELIMINADOFUENTE = 'N' AND A.CODPRODUCTO IN ('CPEEFM','CPECMC','CPEDPP','CPECEM','CPEECV','CPEGEN','CPEADH','CPEFIA') AND CAST(date_format(A.FECAPERTURA, 'yyyyMM') AS INT) >= 202001
),
VENTASCEF_UNICAS AS (
	SELECT
		CODMES,
		FECAPERTURA,
		FECDESEMBOLSO,
		CODCLAVECTA,
		CODSOLICITUD,
		CODCLAVEPARTYCLI,
		CODCLAVEUNICOCLI,
		CODINTERNOCOMPUTACIONAL,
		CODPRODUCTO,
		MTODESEMBOLSADO,
		CODMONEDA,
		MTODESEMBOLSADOSOL,
		CODSOLICITUDCORTO,
		CANAL
	FROM (
		SELECT
			*,
			ROW_NUMBER() OVER (
				PARTITION BY CODMES, CODCLAVECTA
				ORDER BY CODCLAVECTA
			) AS  RN
		FROM VENTASCEF
	)
	WHERE RN = 1
)
SELECT
	CODMES,
	FECAPERTURA,
	FECDESEMBOLSO,
	CODCLAVECTA,
	CODSOLICITUD,
	CODCLAVEPARTYCLI,
	CODCLAVEUNICOCLI,
	CODINTERNOCOMPUTACIONAL,
	CODPRODUCTO,
	MTODESEMBOLSADO,
	CODMONEDA,
	MTODESEMBOLSADOSOL,
	CODSOLICITUDCORTO,
	CANAL
FROM VENTASCEF_UNICAS








CREATE TABLE CATALOG_LHCL_PROD_BCP_EXPL.BCP_EDV_RBMBDN.T72496_VENTAS_SOLICITUDES_CEF
AS
WITH
M_SOLICITUDCREDITOCONSUMO AS (
	SELECT
		CAST(date_format(FECSOLICITUD, 'yyyyMM') AS INT) AS CODMES,
		CODSOLICITUD,
		FECSOLICITUD,
		HORSOLICITUD,
		CODINTERNOCOMPUTACIONAL,
		DESTIPESTADOSOLICITUD,
		FLGDESEMBOLSOSOLICITUD,
		FECULTESTADOSOLICITUD,
		CODPRODUCTO,
		CODAPPSOLICITUD,
		FLGVENTACEF,
		FLGCOMPRADEUDA,
		FLGCOMPRADEUDAADJUNTA,
		MTOSOLICITADOCDD,
		DESTIPRESULTADO,
		DESPRODUCTO,
		CODMONEDA,
		NUMDIAPAGOCUOTA,
		MTOSOLICITADO,
		CTDPLAZOSOLICITADO,
		PCTTASAEFECTIVAANUALSOLICITUD,
		MTOCUOTAMENSUALSOLICITADO,
		MTOAPROBADO,
		CTDPLAZOAPROBADO,
		PCTTASAEFECTIVAANUALAPROBADA,
		MTOCUOTAMENSUALAPROBADO,
		CODMATRICULACOLABORADORANALISTA,
		DESTIPETAPA
	FROM CATALOG_LHCL_PROD_BCP.BCP_UDV_INT_VU.M_SOLICITUDCREDITOCONSUMO
	WHERE CAST(date_format(FECSOLICITUD, 'yyyyMM') AS INT) >= 202301
)
SELECT
	A.CODMESEVALUACION,
	A.CODEVALUACIONSOLICITUD,
	A.FECEVALUACION,
	A.CODINTERNOCOMPUTACIONAL,
	A.NUMSOLICITUDEVALUACION,
	A.DESDECISIONEVALUACION,
	A.MTOCEMEVALUACION,
	A.DESCAMPANIASOLICITUD,
	A.DESTIPEVALUACIONSOLICITUDCREDITO,
	A.DESCANALVENTARBMPER,
	A.NUMSOLICITUDCORTO,
	A.RESULTADOJERARQUIA,
	A.EVALUACIONREACTIVO,
	A.LEAD_CEF,
	B.FECAPERTURA,
	B.FECDESEMBOLSO,
	B.CODCLAVECTA,
	B.CODSOLICITUD AS CODSOLICITUDVENTA,
	B.CODCLAVEPARTYCLI,
	B.CODPRODUCTO,
	B.MTODESEMBOLSADO,
	B.MTODESEMBOLSADOSOL,
	B.CODSOLICITUDCORTO,
	B.CANAL,
	C.CODSOLICITUD,
	C.FECSOLICITUD,
	C.CODPRODUCTO,
	C.DESPRODUCTO,
	C.FLGVENTACEF,
	C.MTOSOLICITADO,
	C.MTOAPROBADO
FROM CATALOG_LHCL_PROD_BCP_EXPL.BCP_EDV_RBMBDN.T72496_SOLICITUDESCEF A
LEFT JOIN CATALOG_LHCL_PROD_BCP_EXPL.BCP_EDV_RBMBDN.T72496_VENTASCEF B ON (A.NUMSOLICITUDCORTO = B.CODSOLICITUDCORTO AND A.DESCANALVENTARBMPER = B.CANAL AND A.CODINTERNOCOMPUTACIONAL = B.CODINTERNOCOMPUTACIONAL)
LEFT JOIN M_SOLICITUDCREDITOCONSUMO C ON (A.CODMESEVALUACION = C.CODMES AND A.NUMSOLICITUDEVALUACION = C.CODSOLICITUD)










CREATE TABLE CATALOG_LHCL_PROD_BCP_EXPL.BCP_EDV_RBMBDN.T72496_BASELEADS
AS
WITH
HD_CONSOLIDADOCAMPANIACRM AS (
	SELECT
		CODMESCAMPANIA,
		CODINTERNOCOMPUTACIONAL,
		CODPRODUCTOVENTACRM,
		TIPOFERTACRM,
		DESCAMPANIACRM,
		CODCONDICIONCLIRBM,
		MTOOFERTASOL,
		CTDMESPLAZO,
		PCTTASAEFECTIVAANUAL,
		(CASE
			WHEN CODPRODUCTOVENTACRM = '0006' AND TIPOFERTACRM = '0003' AND CODCONDICIONCLIRBM = 'INV' THEN 'CEF LD INVITADO'
			WHEN CODPRODUCTOVENTACRM = '0006' AND TIPOFERTACRM = '0003' AND CODCONDICIONCLIRBM = 'PRE' THEN 'CEF LD PREAPROBADO'
			WHEN CODPRODUCTOVENTACRM = '0006' AND TIPOFERTACRM = '0003' AND CODCONDICIONCLIRBM = 'APR' THEN 'CEF LD 100% APROBADO'
			WHEN CODPRODUCTOVENTACRM = '0006' AND TIPOFERTACRM = '0003' AND CODCONDICIONCLIRBM = 'OPT' THEN 'CEF LD OPTIMUS'
			WHEN CODPRODUCTOVENTACRM = '0006' AND TIPOFERTACRM = '0098' THEN 'CEF SHIELD'
			WHEN CODPRODUCTOVENTACRM = '0006' AND TIPOFERTACRM = '0041' AND CODCONDICIONCLIRBM = 'PRE' THEN 'CEF CDD PREAPROBADO'
			WHEN CODPRODUCTOVENTACRM = '0006' AND TIPOFERTACRM = '0041' AND CODCONDICIONCLIRBM = 'APR' THEN 'CEF CDD 100% APROBADO'
			WHEN CODPRODUCTOVENTACRM = '0006' AND TIPOFERTACRM = '0187' THEN 'CEF CDD INSUPERABLE'
			WHEN CODPRODUCTOVENTACRM = '0006' AND TIPOFERTACRM = '0038' AND CODCONDICIONCLIRBM = 'PRE' THEN 'CEF RE PREAPROBADO'
			WHEN CODPRODUCTOVENTACRM = '0006' AND TIPOFERTACRM = '0038' AND CODCONDICIONCLIRBM = 'APR' THEN 'CEF RE 100% APROBADO'
			WHEN CODPRODUCTOVENTACRM = '0110' AND TIPOFERTACRM = '0003' THEN 'CEF CONVENIO LD'
			WHEN CODPRODUCTOVENTACRM = '0110' AND TIPOFERTACRM = '0041' THEN 'CEF CONVENIO CDD'
			WHEN CODPRODUCTOVENTACRM = '0110' AND TIPOFERTACRM = '0038' THEN 'CEF CONVENIO RE'
			WHEN CODPRODUCTOVENTACRM = '0127' AND TIPOFERTACRM = '0003' THEN 'CEF MULTI LD'
		END) AS PRODUCTO,
		ROW_NUMBER() OVER (
			PARTITION BY CODMESCAMPANIA, CODINTERNOCOMPUTACIONAL, CODPRODUCTOVENTACRM, TIPOFERTACRM
			ORDER BY FECINICIOVIGENCIACAMPANIA
		) AS N
	FROM CATALOG_LHCL_PROD_BCP.BCP_DDV_CRM_CAMPANIAMASIVA_VU.HD_CONSOLIDADOCAMPANIACRM
	WHERE CODPRODUCTOVENTACRM IN ('0006', '0110', '0127') AND TIPOFERTACRM IN ('0003', '0038', '0041', '0098', '0187')
),
HD_CONSOLIDADOCAMPANIACRM_UNICOS AS (
	SELECT
		CODMESCAMPANIA,
		CODINTERNOCOMPUTACIONAL,
		CODPRODUCTOVENTACRM,
		TIPOFERTACRM,
		DESCAMPANIACRM,
		CODCONDICIONCLIRBM,
		MTOOFERTASOL,
		CTDMESPLAZO,
		PCTTASAEFECTIVAANUAL,
		(CASE
			WHEN CODPRODUCTOVENTACRM = '0006' AND TIPOFERTACRM = '0003' AND CODCONDICIONCLIRBM IN ('APR', 'PRE') THEN 1
			WHEN CODPRODUCTOVENTACRM = '0006' AND TIPOFERTACRM = '0003' AND CODCONDICIONCLIRBM = 'OPT' THEN 2
			WHEN CODPRODUCTOVENTACRM = '0006' AND TIPOFERTACRM = '0041' AND CODCONDICIONCLIRBM IN ('APR', 'PRE') THEN 3
			WHEN CODPRODUCTOVENTACRM = '0006' AND TIPOFERTACRM = '0038' AND CODCONDICIONCLIRBM IN ('APR', 'PRE') THEN 4
			WHEN CODPRODUCTOVENTACRM = '0006' AND TIPOFERTACRM = '0098' THEN 5
			WHEN CODPRODUCTOVENTACRM = '0006' AND TIPOFERTACRM = '0003' AND CODCONDICIONCLIRBM = 'INV' THEN 6
			WHEN CODPRODUCTOVENTACRM = '0006' AND TIPOFERTACRM = '0187' THEN 7
			WHEN CODPRODUCTOVENTACRM = '0110' AND TIPOFERTACRM = '0003' THEN 8
			WHEN CODPRODUCTOVENTACRM = '0110' AND TIPOFERTACRM = '0041' THEN 9
			WHEN CODPRODUCTOVENTACRM = '0110' AND TIPOFERTACRM = '0038' THEN 10
			WHEN CODPRODUCTOVENTACRM = '0127' AND TIPOFERTACRM = '0003' THEN 11
			ELSE 12
		END) AS PRIORIDADLEAD,
		(CASE
			WHEN CODPRODUCTOVENTACRM = '0006' AND TIPOFERTACRM = '0003' AND CODCONDICIONCLIRBM IN ('APR', 'PRE') THEN 'LD APROBADO'
			WHEN CODPRODUCTOVENTACRM = '0006' AND TIPOFERTACRM = '0003' AND CODCONDICIONCLIRBM = 'OPT' THEN 'LD OPT'
			WHEN CODPRODUCTOVENTACRM = '0006' AND TIPOFERTACRM = '0041' AND CODCONDICIONCLIRBM IN ('APR', 'PRE') THEN 'CDD APROBADO'
			WHEN CODPRODUCTOVENTACRM = '0006' AND TIPOFERTACRM = '0038' AND CODCONDICIONCLIRBM IN ('APR', 'PRE') THEN 'REE APROBADO'
			WHEN CODPRODUCTOVENTACRM = '0006' AND TIPOFERTACRM = '0098' THEN 'LD SHIELD'
			WHEN CODPRODUCTOVENTACRM = '0006' AND TIPOFERTACRM = '0003' AND CODCONDICIONCLIRBM = 'INV' THEN 'LD INV'
			WHEN CODPRODUCTOVENTACRM = '0006' AND TIPOFERTACRM = '0187' THEN 'CDD INS'
			WHEN CODPRODUCTOVENTACRM = '0110' AND TIPOFERTACRM = '0003' THEN 'LD CONVENIO'
			WHEN CODPRODUCTOVENTACRM = '0110' AND TIPOFERTACRM = '0041' THEN 'CDD CONVENIO'
			WHEN CODPRODUCTOVENTACRM = '0110' AND TIPOFERTACRM = '0038' THEN 'REE CONVENIO'
			WHEN CODPRODUCTOVENTACRM = '0127' AND TIPOFERTACRM = '0003' THEN 'LD MULTI'
			ELSE 'OTRO'
		END) AS LEADCEF
	FROM HD_CONSOLIDADOCAMPANIACRM
	WHERE N = 1 AND PRODUCTO IS NOT NULL
),
HD_CONSOLIDADOCAMPANIACRM_CONSOLIDADO AS (
	SELECT *
	FROM HD_CONSOLIDADOCAMPANIACRM_UNICOS
	QUALIFY ROW_NUMBER() OVER (
		PARTITION BY CODMESCAMPANIA, CODINTERNOCOMPUTACIONAL, LEADCEF
		ORDER BY CODINTERNOCOMPUTACIONAL
	) = 1
),
HD_CONSOLIDADOCAMPANIACRM_LEADS_UNICOS AS (
	SELECT *
	FROM HD_CONSOLIDADOCAMPANIACRM_CONSOLIDADO
	QUALIFY ROW_NUMBER() OVER (
		PARTITION BY CODMESCAMPANIA, CODINTERNOCOMPUTACIONAL
		ORDER BY PRIORIDADLEAD
	) = 1
)
SELECT *
FROM HD_CONSOLIDADOCAMPANIACRM_LEADS_UNICOS
















CREATE OR REPLACE TABLE CATALOG_LHCL_PROD_BCP_EXPL.BCP_EDV_RBMBDN.T72496_SOLICITUDES_LEAD AS
WITH
VENTAS_SOLICITUDES AS (
    SELECT *
    FROM CATALOG_LHCL_PROD_BCP_EXPL.BCP_EDV_RBMBDN.T72496_VENTAS_SOLICITUDES_CEF
),
BASELEADS AS (
    SELECT
        CODMESCAMPANIA,
        TRIM(CODINTERNOCOMPUTACIONAL) AS CODINTERNOCOMPUTACIONAL,
        CODPRODUCTOVENTACRM,
        TIPOFERTACRM,
        DESCAMPANIACRM,
        CODCONDICIONCLIRBM,
        MTOOFERTASOL,
        CTDMESPLAZO,
        PCTTASAEFECTIVAANUAL,
        PRIORIDADLEAD,
        LEADCEF
    FROM CATALOG_LHCL_PROD_BCP_EXPL.BCP_EDV_RBMBDN.T72496_BASELEADS
),

SOLICITUDES_LEAD_NO_REACTIVO AS (
    SELECT
        a.*,
        b.CODMESCAMPANIA AS CODMES_LEAD,
        b.CODPRODUCTOVENTACRM AS TIPVENTA,
        b.TIPOFERTACRM AS TIPOFERTA,
        b.DESCAMPANIACRM AS DESCAMPANALEAD,
        b.CODCONDICIONCLIRBM AS CODCONDICIONCLIENTE,
        b.MTOOFERTASOL AS MTOFINALOFERTADOSOL,
        b.CTDMESPLAZO AS PLAZO,
        b.PCTTASAEFECTIVAANUAL AS TEA
    FROM VENTAS_SOLICITUDES a
    LEFT JOIN BASELEADS b
      ON TRIM(a.CODINTERNOCOMPUTACIONAL) = b.CODINTERNOCOMPUTACIONAL
     AND a.CODMESEVALUACION = b.CODMESCAMPANIA
     AND a.LEAD_CEF = b.LEADCEF
    WHERE a.LEAD_CEF NOT IN ('REACTIVO', 'CONS APROBADO', 'CONS CONVENIO')
),

SOLICITUDES_LEAD_REACTIVO AS (
    SELECT
        x.*,
        y.CODMESCAMPANIA AS CODMES_LEAD,
        y.CODPRODUCTOVENTACRM AS TIPVENTA,
        y.TIPOFERTACRM AS TIPOFERTA,
        y.DESCAMPANIACRM AS DESCAMPANALEAD,
        y.CODCONDICIONCLIRBM AS CODCONDICIONCLIENTE,
        y.MTOOFERTASOL AS MTOFINALOFERTADOSOL,
        y.CTDMESPLAZO AS PLAZO,
        y.PCTTASAEFECTIVAANUAL AS TEA
    FROM VENTAS_SOLICITUDES x
    LEFT JOIN BASELEADS y
      ON TRIM(x.CODINTERNOCOMPUTACIONAL) = y.CODINTERNOCOMPUTACIONAL
     AND x.CODMESEVALUACION = y.CODMESCAMPANIA
    WHERE x.LEAD_CEF IN ('REACTIVO', 'CONS APROBADO', 'CONS CONVENIO')
)

SELECT * FROM SOLICITUDES_LEAD_NO_REACTIVO
UNION ALL
SELECT * FROM SOLICITUDES_LEAD_REACTIVO;
